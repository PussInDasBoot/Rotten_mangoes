<nav class="navbar navbar-default">
  <div class="container">
    <span class="navbar-right">
    <% if current_user %>
        <p class="navbar-text">Signed in as <%=current_user.full_name%></p>
        <%= link_to "Log out", session_path("current"), class: "btn btn-default navbar-btn", method: :delete %>
      <%if current_user.admin %>
        <%= link_to "Admin", admin_users_path, class: "btn btn-default navbar-btn" %>
      <% end %>
    <% else %>
      <%= link_to "Sign up", new_user_path, class: "btn btn-default navbar-btn" %> |
      <%= link_to "Log in", new_session_path, class: "btn btn-default navbar-btn" %>
    <% end %>
    </span>
    <%= link_to "Home", movies_path, class: "btn btn-default navbar-btn" %>
  </div>
</nav>